@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

@{
    ViewBag.Title = "All Roles";
    Layout = "_AppLayout";
}

<h1>All Roles</h1>

@if (Model.Any())
{
    <a href="#"
       class="btn btn-primary mb-3" style="width:auto">
        Add new Role
    </a>

    <div class="col-lg-12 grid-margin stretch-card">
           <div class="card">
            <div class="card-body">
     
              <div class="row">
                <div class="col-12">
                  <div class="table-responsive table table-hover table-striped ">
                    <table id="order-listing" class="table">
                      <thead>
                        <tr>
                            <th>#</th>
                            <th>Name</th>
                            <th>Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                      @foreach (var user in Model)
                      {
                          <tr>
                              <td>@user.Id</td>
                              <td>@user.Name</td>
                              <td>
                                  <a asp-controller="Order" asp-action="View" asp-route-Id="@user.Id" class="btn btn-sm btn-outline-primary my-1">View</a>
                                  <a asp-controller="Order" asp-action="Update" asp-route-Id="@user.Id" class="btn btn-sm btn-secondary my-1">Update</a>
                                  <a asp-controller="Order" asp-action="Delete" asp-route-Id="@user.Id" class="btn btn-sm btn-danger my-1">Delete</a>
                              </td>
                          </tr>
                      }
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
}
else
{
    <div class="card">
        <div class="card-header">
            No roles created yet
        </div>
        <div class="card-body">
            <h5 class="card-title">
                Use the button below to create a role
            </h5>
            
        </div>
    </div>
}